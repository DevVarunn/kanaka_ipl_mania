import { GetteaminfoPipe } from './getteaminfo.pipe';

describe('GetteaminfoPipe', () => {
  it('create an instance', () => {
    const pipe = new GetteaminfoPipe();
    expect(pipe).toBeTruthy();
  });
});
