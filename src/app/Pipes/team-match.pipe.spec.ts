import { TeamMatchPipe } from './team-match.pipe';

describe('TeamMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
